# extra-store

This is a small static storefront prototype for extra store — built with plain HTML, Tailwind CSS, and small client-side JavaScript.

Quick Start
1. Serve locally:
```bash
python3 -m http.server 8000

# then open http://localhost:8000 in your browser
```

2. Pages:
- `index.html` — home and product grid
- `product.html` — product listing page
- `cart.html` — cart modal page (reads cart from localStorage)

Codespaces
 - Open in GitHub Codespaces: https://github.com/codespaces/new/alvarezthectreator/extra-store

Push to GitHub
If you want me to push these files to your GitHub repo, run these commands locally (or allow me to run them with network access):

```bash
git init
git add .
git commit -m "Add site files and README"
git branch -M main
git remote add origin https://github.com/alvarezthectreator/extra-store.git
git push -u origin main
```

If the repository already exists and you prefer a one-off upload via the web UI, use GitHub's Upload files workflow.

Licensing
This project includes a MIT license by default (see `LICENSE`).

Contact
If you'd like, I can attempt to run the `git push` for you from this environment — say "yes, push" and I'll request network permission and proceed. Otherwise, run the commands above locally.
